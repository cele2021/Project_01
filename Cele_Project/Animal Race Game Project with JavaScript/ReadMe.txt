#Points:
1).There are four different animal images positioned at the starting line of a race track.

2.)Clicking the "Start Race" button triggers the startRace() function, which starts the race.

3).Each animal moves randomly with a speed between 5 and 15 pixels per 100 milliseconds. 

4).The race ends when one of the animals reaches the finish line, and an alert is shown declaring the winner.